const squareAndAdd = (a, b) => {
  return a ** 2 + b ** 2;
};

/* Only one export default allowed
export default const divAndMult = (a, b) => {
  return a ** 2 + b ** 2;
};
*/

// When you have only ONE thing to export
export default squareAndAdd;
